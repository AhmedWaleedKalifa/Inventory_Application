<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h2><%= title %></h2>
    <main>
        <div class="container">
            <div class="items_inner_container">
                <% if(locals.category){%>
                    <h3><%= category.name%></h3>
                    <p>Description: <%= category.description%></p>
                    <ul>
                        <% items.forEach(item=>{ %>
                            <%-include('itemCard.ejs',{item:item,categoryId:category.id})%>
                        <% }) %>
                    </ul>
                    
                    <% }%>
            </div>
        </div>
    </main>
    
</body>
</html>